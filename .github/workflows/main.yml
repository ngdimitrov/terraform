name: Deploy to Production

on:
  push:
    branches:
      - main
      - terraform-conf
      - develop
  pull_request:
    branches:
      - main
      - terraform-conf
      - develop


jobs:
  build:
    name: Deploy Application
    runs-on: self-hosted
    
    defaults:
      run:
        shell: bash


    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: create
        run: sudo touch /opt/test18.txt
